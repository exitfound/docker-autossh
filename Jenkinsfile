pipeline { 
    agent {
        docker 'nginx:latest'
    }
       stages { 
           stage ('Build') { 
               steps { 
                   echo 'Running build phase. ' 
               }
           }
       }
   }
